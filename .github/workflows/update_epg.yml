name: Update EPG

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  build_epg:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # <-- FONDAMENTALE: permette al bot di fare il commit e il push

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run EPG script
        run: |
          python aggiorna_epg.py

      - name: Commit and push updated EPG
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add epg_sky.xml  # Metti il nome esatto del file XML generato dal tuo script Python
          git commit -m "EPG Sky auto-update" || echo "Nothing to commit"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
